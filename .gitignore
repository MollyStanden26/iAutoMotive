node_modules/
.next/
out/
.env
.env.local
.env.production.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.claude/worktrees/

# Stray Next tmp build dir produced when TMPDIR is short-circuited into the worktree
var/

# Local-filesystem fallback for the upload pipeline. In prod everything
# lands on Vercel Blob (BLOB_READ_WRITE_TOKEN); these directories only
# exist when running without that token and they're dev artefacts, not
# data we want shipped in git.
public/uploads/contracts/
public/uploads/vehicles/

# Vercel artifacts
.vercel/
.vercel
