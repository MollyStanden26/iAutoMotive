// Account settings — profile, notifications, password, delete account

export default function SettingsPage() {
  return (
    <div>
      <h1>Account Settings</h1>
      <p>Profile, notifications, password, delete account</p>
    </div>
  );
}
