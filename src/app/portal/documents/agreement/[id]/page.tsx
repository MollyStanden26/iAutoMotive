// Consignment agreement — signed agreement viewer, key terms, download

export default function ConsignmentAgreementPage({
  params,
}: {
  params: { id: string };
}) {
  return <div>Consignment Agreement — {params.id}</div>;
}
