// Onboarding checklist — V5C, finance disclosure, service history, spare keys

export default function OnboardingChecklistPage({
  params,
}: {
  params: { offerId: string };
}) {
  return <div>Onboarding Checklist — {params.offerId}</div>;
}
