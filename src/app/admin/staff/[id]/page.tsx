// Staff member detail — deals, recon throughput, CRM stats, training

export default function StaffDetailPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Staff Member Detail</h1>
    </div>
  );
}
