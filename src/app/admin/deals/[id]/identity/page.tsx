// Identity & fraud — risk score, ID verification, deepfake flags

export default function DealIdentityPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Identity &amp; Fraud</h1>
    </div>
  );
}
