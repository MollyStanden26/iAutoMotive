// Finance submission — lender status, stips, funding ETA

export default function DealFinancePage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Finance Submission</h1>
    </div>
  );
}
