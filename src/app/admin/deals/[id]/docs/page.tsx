// Document manager — all deal docs, signing status, missing items

export default function DealDocsPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Document Manager</h1>
    </div>
  );
}
