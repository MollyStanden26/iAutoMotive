// Deal detail — buyer, vehicle, finance, identity, documents, timeline

export default function DealDetailPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Deal Detail</h1>
    </div>
  );
}
