// Vehicle detail (admin) — timeline, photos, recon, pricing, documents

export default function InventoryVehicleDetailPage({
  params,
}: {
  params: { vin: string };
}) {
  return (
    <div>
      <h1>Vehicle Detail</h1>
    </div>
  );
}
