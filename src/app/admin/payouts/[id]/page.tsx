// Payout detail — net sheet, escrow conditions, release button

export default function PayoutDetailPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Payout Detail</h1>
    </div>
  );
}
