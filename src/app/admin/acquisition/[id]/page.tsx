// Lead detail — seller info, vehicle, Scout score, AI outreach log, offer history

export default function AcquisitionLeadDetailPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Lead Detail</h1>
    </div>
  );
}
