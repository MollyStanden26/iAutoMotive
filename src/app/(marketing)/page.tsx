// Homepage — hero + reg bar, proof strip, browse by body type, featured listings,
// how it works, values/proof, CTA band, footer
export default function HomePage() {
  return <div>Homepage</div>;
}
