// Vehicle detail page (VDP) — 360 spin, photos, HPI, condition report, AI chat, reserve
export default function VehicleDetailPage({
  params,
}: {
  params: { id: string; slug: string };
}) {
  return <div>Vehicle Detail Page</div>;
}
