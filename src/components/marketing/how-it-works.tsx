// How it works — white bg, 48px vertical padding, 4-column step grid
// Slate 50 step cards with 20px radius, Teal 600 numbered circles (PJS 800, white text)
// Stagger animation: 100ms delay per column, capped at 400ms
export function HowItWorks() {
  return <section>How It Works</section>;
}
