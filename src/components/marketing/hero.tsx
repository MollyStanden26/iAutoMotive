// Hero section — Teal 600 full-width bg, PJS 800 display headline
// Registration bar (white, 14px radius, navy submit button)
// Trust chips (dark semi-transparent pill, white text, dark teal checkmarks)
export function Hero() {
  return <section>Hero</section>;
}
