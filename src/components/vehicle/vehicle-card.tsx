// Vehicle listing card — white bg, 1.5px Slate 100 border, 20px radius
// Overflow hidden for image. PJS card titles. Footer strip: Slate 50 bg.
// Hover: translateY(-4px) + border Teal 400 + shadow
export function VehicleCard() {
  return <div>Vehicle Card</div>;
}
