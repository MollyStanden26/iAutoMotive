// Scroll reveal hook — IntersectionObserver at threshold 0.15
// Respects prefers-reduced-motion
// Types: fade-up (default), fade-in, scale-in, slide-right
// Stagger: 100ms per column, capped at 400ms
export {};
